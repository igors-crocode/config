import styled from '@emotion/styled'

const QuoteSectionEmptyText = styled.p`
	font-weight: 400;
	font-size: 16px;
	line-height: 150%;
	color: ${({theme}) => theme.palette.text.primary};
`

export default QuoteSectionEmptyText

import styled from '@emotion/styled'

const QuoteSectionMessageLabel = styled.label`
	font-size: 16px;
	line-height: 150%;
	color: ${({theme}) => theme.palette.text.primary};
`

export default QuoteSectionMessageLabel

import styled from '@emotion/styled'

const QuoteSectionWrapper = styled.section`
	display: flex;
	flex-direction: column;
	flex-grow: 1;
	gap: 40px;
	justify-content: space-between;
`

export default QuoteSectionWrapper

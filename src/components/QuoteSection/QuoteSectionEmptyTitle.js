import styled from '@emotion/styled'

const QuoteSectionEmptyTitle = styled.h2`
	font-weight: 500;
	font-size: 20px;
	line-height: 150%;
	color: ${({theme}) => theme.palette.text.primary};
`

export default QuoteSectionEmptyTitle

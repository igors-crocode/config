import styled from '@emotion/styled'

const QuoteSectionEmptyWrapper = styled.div`
	background-color: ${({theme}) => theme.palette.background.secondary};
	display: flex;
	flex-direction: column;
	padding: 40px 20px;
	gap: 10px;
`

export default QuoteSectionEmptyWrapper
